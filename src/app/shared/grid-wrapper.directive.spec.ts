import { GridWrapperDirective } from './grid-wrapper.directive';

describe('GridWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new GridWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
